<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls">
    <BoxContainer Name="VBox" Orientation="Vertical">
        <controls:NanoHeading Name="Header" Text="{Loc 'lobby-character-preview-panel-header'}">

        </controls:NanoHeading>
        <BoxContainer Name="Loaded" Orientation="Vertical" Visible="False">
            <Label Name="Summary" HorizontalAlignment="Center" Margin="3 3"/>
            <BoxContainer Name="Carousel" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextureButton Name="PreviousButton"
                               TexturePath="left_arrow.svg.192dpi.png"
                               VerticalAlignment="Center"
                               Visible="False"/>
                <LayoutContainer Name="ViewBox" MinSize="200 116" InheritChildMeasure="False"/>
                <TextureButton Name="NextButton"
                               TexturePath="right_arrow.svg.192dpi.png"
                               VerticalAlignment="Center"
                               Visible="False"/>
            </BoxContainer>
            <controls:VSpacer/>
            <Button Name="CharacterSetup" Text="{Loc 'lobby-character-preview-panel-character-setup-button'}"
                HorizontalAlignment="Center" Margin="0 5 0 0"/>
        </BoxContainer>
        <Label Name="Unloaded" Text="{Loc 'lobby-character-preview-panel-unloaded-preferences-label'}"/>
    </BoxContainer>
</Control>
